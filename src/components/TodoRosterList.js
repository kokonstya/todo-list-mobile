import React from 'react'
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native'

const TodoRosterList = ({  }) => {
    return (
        <TouchableOpacity
            activeOpacity={0.5}
        >
            <View style={styles.todoRosterList}>

            </View>
        </TouchableOpacity>
    )
}

const styles = StyleSheet.create({
    todoRosterList: {

    }
})

export default TodoRosterList;
